name: download_task
description: Resumable http download request - pause, resume, cancel, realtime progress and error handling
repository: https://github.com/starkdmi/resumable_download
version: 1.0.4

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  http: '>=0.13.0 <2.0.0'

dev_dependencies:
  lints: '>=0.1.0 <3.0.0'
  test: '>=1.16.0 <2.0.0'